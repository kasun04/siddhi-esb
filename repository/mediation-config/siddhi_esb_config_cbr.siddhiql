define stream inFlow ( ptcontext string, receivingFlow string, nextFlow string); from inFlow [siddhiESB:evalXPath('1', '2') == '<symbol>kasun</symbol>' ] select ptcontext, 'http://localhost:9000/service/EchoService' as endpoint, receivingFlow insert into sender;